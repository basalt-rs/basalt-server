[package]
name = "basalt-server"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.23", features = ["derive"] }
protoxene = { git = "https://github.com/basalt-rs/protoxene.git" }
